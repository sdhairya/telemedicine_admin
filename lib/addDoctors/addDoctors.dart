import 'package:flutter/material.dart';
import 'package:telemedicine_admin/addDoctors/body.dart';

class addDoctors extends StatelessWidget {
  const addDoctors({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return body();
  }
}
